```
'y'       // yes，替换
'n'       // no，不替换，掉过 to skip this match
'a'       // all，替换当前和剩下所有
'q'       // quit，不替换，退出。
'l'       // last，最后一次替换，替换后退出
CTRL-E    // 屏幕向上滚动，如果匹配项在最后一行但需要关联查看下一行就很有用。
CTRL-Y    // 屏幕向下滚动，如果匹配项在第一行却需要关联查看上一行就很有用。
````
